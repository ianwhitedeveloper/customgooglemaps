/* inject:components */

/* endinject */
@import './components/variables.styl'
@import './components/inputs.styl'
@import './components/google-container.styl'
@import './components/google-map.styl'
@import './components/results.styl'
@import './components/search.styl'
@import './components/footer.styl'
@import './components/ad-space.styl'
/* -------------------------------- 

Primary style

-------------------------------- */
html, body
  height auto

html
  *
    -webkit-font-smoothing antialiased
    -moz-osx-font-smoothing grayscale

body
  position relative
  -webkit-font-smoothing antialiased
  font-family 'Nunito', sans-serif
  font-size 13px
  color #2d313f
  margin 0
  background: repeating-linear-gradient(
    -45deg,
    #fff 6px,
    #fff 11px,
    rgba(240, 240, 241, 0.5) 0px,
    rgba(240, 240, 241, 0.5) 13px
  )
 
h1, h2, h3, h4, h5, h6, p
	margin 0
	padding 0

h1 
	font-size 31px
h2
	font-size 20px
h3 
	font-size 16px
h4
	font-size 12px

.text_center 
	text-align center

*, *:after, *:before
  -webkit-box-sizing border-box
  -moz-box-sizing border-box
  box-sizing border-box
  
main
	position relative
	width 100%
	height auto
	margin-bottom -150px

	&:after
		content ""
		display block
		height 150px
	
#mask 
	position absolute
	width 100%
	height 100%
	background-color transparent
	z-index -1
	-webkit-transition: background 0.2s linear;
	-moz-transition: background 0.2s linear;
	-ms-transition: background 0.2s linear;
	-o-transition: background 0.2s linear;
	transition: background 0.2s linear;

.desktop
	display none

.mobile
	display block
	
.flex
	display(flex)
	*
		display(flex auto)
		flex-align-self(center)
	
@media only screen and (min-width: 1024px)
	html, body
	  height 100%
	  
	body
		position static
		font-size 14px
	  
		h1 
			font-size 46px
		h2
			font-size 30px
		h3 
			font-size 21px
		h4
			font-size 15px
		
	main
		margin-bottom -85px
		
		&:after
			height 85px

	.desktop
		display block
	
	.mobile
		display none
	